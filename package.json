{
  "name": "fe-1",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "lite-server",
    "server": "json-server --watch src/api/mock-db.json --port 3002",
    "dev": "concurrently \"npm run server\" \"npm run start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  },
  "dependencies": {
    "concurrently": "^9.2.0",
    "json-server": "^1.0.0-beta.3"
  }
}
